# rc.conf

export PERPD_OPTS="-a 0"

# export KEYMAP="us"
export LOG_USER="tinylog"
export LOG_CMD="tinylog -k 8 -s 32000000 -t -z"
export TINYLOG_ZIP="xz"

exec_supervisor() {
    exec perpboot /etc/perp
}

# vim: ft=sh

